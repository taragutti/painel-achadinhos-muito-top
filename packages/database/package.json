{
  "name": "@achadinhos/database", "version": "0.1.0", "private": true, "type": "module",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
    "./auth": { "types": "./dist/auth.d.ts", "default": "./dist/auth.js" }
  },
  "scripts": {
    "generate": "prisma generate", "migrate:dev": "prisma migrate dev", "admin:upsert": "npm run build && node scripts/upsert-admin.mjs", "build": "prisma generate && tsc -p tsconfig.json", "typecheck": "tsc --noEmit", "test": "node --test"
  },
  "dependencies": { "@prisma/client": "^6.16.3" },
  "devDependencies": { "prisma": "^6.16.3", "typescript": "^5.9.3" }
}
